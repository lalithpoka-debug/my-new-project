# Sample Data Folder

Place any `.pcap` files here if you want to test the IDS in offline analysis mode.

Example command:

```powershell
python run_ids.py --pcap sample_data\traffic_capture.pcap
```

